# OpenAI API Key (required for wiki generation)
OPENAI_API_KEY="your_openai_api_key_here"

# Enable user-provided API keys (optional)
ENABLE_USER_API_KEYS="true"

# Redis URL for usage quotas and rate limiting (optional, falls back to in-memory storage)
# Use either Redis URL or Vercel KV URL (KV_REST_API_URL)
REDIS_URL="redis://localhost:6379"

# Vercel KV for rate limiting (production) - get from Vercel dashboard
# KV_REST_API_URL="https://your-kv-id.kv.vercel-storage.com"
# KV_REST_API_TOKEN="your_kv_token_here"

# Legacy: No longer needed - API keys now stored securely in Supabase Vault
# API_KEY_ENCRYPTION_SECRET="your_encryption_secret_here"

# Free tier daily limit (optional, default: 10)
FREE_TIER_DAILY_LIMIT="10"

# Bypass usage limits for development/testing (optional, default: false)
# WARNING: Only use this for local development. Never enable in production!
BYPASS_USAGE_LIMITS="false"

# Vercel Blob Storage token for storing generated images (optional)
# Get this from your Vercel dashboard: https://vercel.com/docs/storage/vercel-blob
# If not provided, images will be stored as base64 data URLs in the world data
BLOB_READ_WRITE_TOKEN="your_vercel_blob_token_here"

# Supabase configuration for secure API key storage (recommended for production)
# Get these from your Supabase project settings
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"

# Clerk authentication (required)
CLERK_SECRET_KEY="your_clerk_secret_key_here"
VITE_CLERK_PUBLISHABLE_KEY="your_clerk_publishable_key_here"

# CORS allowed origins (production) - comma-separated list
# For development, defaults to localhost:5173,localhost:3000
ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# Sentry error monitoring (optional, production only)
SENTRY_DSN="your_sentry_dsn_here"

# Node environment (auto-set by deployment platforms)
NODE_ENV="production"